services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"

  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - src/.env
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
    restart: unless-stopped
